#!/usr/bin/env python3
"""Entry point. Lives in bin/ so the name does not collide with the package."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1]))

from reclaim.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
